#include "main.h"
/**
 * mul - multiplie two integers and return the result
 * @a: first interger
 * @b: second interger
 *
 * Return: the multiple of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
